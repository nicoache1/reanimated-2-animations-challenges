import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    height: 60,
    paddingHorizontal: 20,
  },
  row: {
    alignItems: 'center',
    flex: 1,
    flexDirection: 'row',
  },
})
